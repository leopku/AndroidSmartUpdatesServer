<!DOCTYPE html>
<html>
  <head>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= content_for?(:title) ? yield(:title) : "Android Smart Updates" %></title>
    <meta name="description" content="<%= content_for?(:description) ? yield(:description) : "Android Smart Updates" %>">
    <%= stylesheet_link_tag "application", :media => "all" %>
    <%= csrf_meta_tags %>
    <%= yield(:head) %>
  </head>
  <body class="<%= controller_name %> <%= action_name %>">

          <%= render 'layouts/navigation' %>
          <div class="container">
            <div class="row">
              <div class="col-sm-12">
                <%= render 'layouts/messages' %>
              </div>
            </div>
          </div>
            
<div class="container">
            <%= yield %>
</div>

        <footer class="container">
          <div class="row">
            <div class="col-sm-12">
              <div class="well">&copy; 2015</div>
            </div>
          </div>
        </footer>
    <%= javascript_include_tag "application" %>
            
  </body>
</html>
